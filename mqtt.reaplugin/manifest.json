{
  "id": "mqtt.reaplugin",
  "author": "peterthepeter; based on work by meldavy",
  "name": "Decaid MQTT Bridge",
  "description": "Publish complete Decaid telemetry and controls to MQTT with optional Home Assistant auto-discovery.",
  "version": "0.2.2",
  "apiVersion": 1,
  "permissions": [
    "log",
    "api",
    "pluginStorage",
    "events.machine",
    "events.shots",
    "network.tcp",
    "network.tls",
    "network.websocket"
  ],
  "drivers": [],
  "api": [],
  "settings": {
    "Host": {
      "type": "string",
      "label": "MQTT broker address",
      "description": "Hostname or IP address, without mqtt:// or a port. Leave empty to disable the bridge."
    },
    "Port": {
      "type": "number",
      "label": "Broker port",
      "default": 8883,
      "description": "MQTT broker port. 8883 for TLS, 1883 for plain TCP."
    },
    "EnableTls": {
      "type": "boolean",
      "label": "Encrypted connection (TLS)",
      "default": true,
      "description": "Use TLS with certificate validation, normally on port 8883. Disable only for a trusted local broker using plain MQTT, normally on port 1883."
    },
    "Username": {
      "type": "string",
      "label": "Username",
      "description": "Broker username (optional)."
    },
    "Password": {
      "type": "string",
      "label": "Password",
      "secure": true,
      "description": "Broker password (optional)."
    },
    "HaAutoDiscoveryEnable": {
      "type": "boolean",
      "label": "Enable Home Assistant auto-discovery",
      "default": false,
      "description": "Publish retained Home Assistant discovery entities. Disable and save before changing brokers or uninstalling."
    },
    "HaDeviceName": {
      "type": "string",
      "label": "Home Assistant device name (optional)",
      "description": "Leave empty to use the connected machine model automatically."
    },
    "HeartbeatSeconds": {
      "type": "number",
      "label": "Status heartbeat (seconds)",
      "default": 60,
      "description": "Regular combined status message while sleeping or disconnected (tablet/Decaid still running). Default: 60 seconds. Wake and connection changes are immediate. Measurements update up to every 1 second during brewing, 2 seconds during heating, and 5 seconds while awake and idle. Minimum: 1 second."
    },
    "TopicPrefix": {
      "type": "string",
      "label": "Advanced: MQTT topic prefix",
      "description": "Normally leave unchanged. Empty uses de1plus/<unique id>. Changing this affects MQTT automations; disable discovery and save before changing it."
    },
    "HaDiscoveryPrefix": {
      "type": "string",
      "label": "Advanced: Home Assistant discovery prefix",
      "default": "homeassistant",
      "description": "Normally leave homeassistant unchanged. Must match Home Assistant's MQTT discovery prefix."
    },
    "HaEntityNamePrefix": {
      "type": "string",
      "label": "Advanced: Home Assistant entity name prefix",
      "default": "DE1+ ",
      "description": "Normally leave DE1+ unchanged. Optional prefix for entity display names, useful with multiple machines."
    },
    "ClientId": {
      "type": "string",
      "label": "Advanced: MQTT client ID",
      "description": "Normally leave empty for a stable, automatically generated ID. Kept for existing installations and brokers requiring a specific ID. Must be unique per client."
    }
  }
}
